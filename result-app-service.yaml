apiVersion: v1
kind: Service
metadata:
  name: result
  labels:
    app: voting-app
spec:
  type: NodePort
  ports:
    - port: 8081
      targetPort: 80
      nodePort: 31001
  selector:
    app: voting-app
    name: result