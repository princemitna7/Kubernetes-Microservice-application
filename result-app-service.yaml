apiVersion: v1
kind: Service
metadata:
  name: result
  labels:
    name: result-service
    app: voting-app
spec:
  type: NodePort
  ports:
    - port: 8081
      targetPort: 80
      nodePort: 31001
  selector:
    name: result
    app: voting-app